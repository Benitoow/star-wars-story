// Single source of truth for the app version. Keep in sync with package.json
// and static/manifest.json on release.
export const APP_NAME = 'Star Wars Story';
export const APP_VERSION = '3.2.2';
export const APP_EDITION = 'Lite';
export const APP_VERSION_LABEL = `v${APP_VERSION} · ${APP_EDITION}`;
